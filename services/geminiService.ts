// Service removed as AI features are no longer needed.
// This file is kept as a placeholder to prevent import errors if referenced elsewhere, 
// though all references should have been removed.

export const generateDeck = async () => {
  throw new Error("AI generation is disabled in this version.");
};
